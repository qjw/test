sudo: required

lang: go

services:
  - docker

before_install:
  - go get -u github.com/kardianos/govendor
  - export PATH="${GOPATH}/bin:${PATH}"
  - export GOPATH="`pwd`:${GOPATH}"
  - govendor sync
  - go build .

script:
  - ls -a
