sudo: required

lang: go

services:
  - docker

before_install:
  - go get -u github.com/kardianos/govendor
  - find ${GOPATH}
  - export vendor=${GOPATH}/bin/govendor
  - pwd
  - ${vendor} sync
  - go build .

script:
  - ls -a
