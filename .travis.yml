sudo: required

lang: go

services:
  - docker

before_install:
  - go get -u github.com/kardianos/govendor
  - find / -name govendor
  - go get github.com/qjw/kelly
  - go build .

script:
  - ls -a
