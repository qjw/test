sudo: required

lang: go

go:
  - "1.8"

services:
  - docker

before_install:
  - go get -u github.com/kardianos/govendor
  - export PATH="${GOPATH}/bin:${PATH}"
  - export ROOT=/tmp/go_$$
  - mkdir -p "${ROOT}" && ln -s "`pwd`" "${ROOT}/src/domain/proj" && cd "${ROOT}/src/domain/proj"
  - pwd
  - find
  - echo ${ROOT}
  - echo ${GOPATH}
  - export GOPATH="${ROOT}:${GOPATH}"
  - govendor sync
  - find
  - go build .

script:
  - ls -a
